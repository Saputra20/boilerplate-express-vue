# Feature Workflow

Read approved task → inspect docs and dependency outputs → implement scope → test behavior → select anti-slop checks → lint/typecheck/build → inspect diff → human review. Do not start next task.
